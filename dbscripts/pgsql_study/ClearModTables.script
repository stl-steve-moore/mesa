#!/bin/csh
#
# CreateModTables
#
if ($1 == "") then
  echo " "
  echo Usage: "$0 <Database Name>"
  echo " "
  exit 1
endif


psql $1 < clearpsview.pgsql
psql $1 < clearvisit.pgsql

if ($2 == "") then
 psql $1 < load_id_mod.pgsql
endif
